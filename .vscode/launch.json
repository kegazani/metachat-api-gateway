{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch API Gateway with Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/main.go",
            "env": {},
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}